### Enforcing Var Keyword
---
Use `"use strict"` mode at the beginning of your JavaScript file or function. This will throw an error if variables are used without being declared using `var`, `let`, or `const`.

